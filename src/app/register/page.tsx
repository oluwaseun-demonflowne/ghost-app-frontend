import Register from "@/src/components/Register";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Register />
    </div>
  );
};

export default page;
